<!-- Page Content -->
<div id="page-wrapper">
    <div class="content" data-pjax-container=""><script>
          $('.nav.nav-pills li.active').removeClass('active');
          $('.nav.nav-pills li[data-model="project"]').addClass('active');
          </script>
            <div class="page-header">
                <h1 class="">Gerenciar Projeto</h1>
            </div>
            <div class="row">
              <div class="col-lg-12">
                <div class="panel panel-default">
                  <div class="panel-heading"> 
                       <i class="fa fa-briefcase fa-fw"></i> 
                       Projeto
                  </div>
                  <!-- /.panel-heading -->
                  <div class="panel-body">
                    <div class="table-responsive">
                      <table class="table table-striped table-bordered table-hover">
                           <thead>
                            <tr>
                              <th>Título</th>
                              <th>Categoria</th>
                              <th>Data Criação</th>
                              <th>Data entrega</th>
                              <th>Categoria</th>
                            </tr>
                          </thead>
                          
                          <tbody>
                              <tr>
                                <td><%= current_project.title %></td>
                                <td><%= current_project.text %></td>
                                <td><%= current_project.delivery_date.strftime("%m/%d/%Y") %></td>
                                <td><%= current_project.creation_date.strftime("%m/%d/%Y") %></td>
                                <td><%= current_project.category.name %></td>
                              </tr>
                          </tbody>
                        </table>
                      </div>
                  </div>
                  <!-- /.panel-body -->
              </div>
          <!-- /.page-panel heading -->
           <!-- /.table-responsive -->
                    <%= render 'report_glossary' %>
                    <%= render 'report_requirement' %>
                    <%= render 'report_use_case' %>
          </div>
          <!-- /.page-panel default -->
        </div>
      </div>
      <!-- row -->
    </div>
    <!-- /.container-fluid -->
</div>
<!-- /#page-wrapper -->

