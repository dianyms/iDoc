<%= form_for(use_case_requirement) do |f| %>
  <% if use_case_requirement.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(use_case_requirement.errors.count, "error") %> prohibited this use_case_requirement from being saved:</h2>

      <ul>
      <% use_case_requirement.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :use_case_id %>
    <%= f.text_field :use_case_id %>
  </div>

  <div class="field">
    <%= f.label :requirement_id %>
    <%= f.text_field :requirement_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
