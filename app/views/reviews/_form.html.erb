<%= form_for(review) do |f| %>
   <% if review.errors.any? %>
    <div class="alert alert-danger alert-dismissable">
      <button type="button" class="close" data-dismiss="alert" aria-hidden="true">×</button>
      <h2><%= pluralize(review.errors.count, "error") %> prohibited this project_user from being saved:</h2>

      <ul>
      <% review.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
  </div>
  <% end %>

  <div class="form-group">
    <label>Data</label>
    <%= f.date_select :data_review, class:"form-control"%>
  </div>

  <div class="form-group">
    <label>Versão</label>
    <%= f.text_field :version, class:"form-control" %>
  </div>

  <div class="form-group">
    <label>Descrição</label>
    <%= f.text_area :description, class:"form-control" %>
  </div>

  <button type="submit" class="btn btn-primary">Salvar<i class="fa fa-check pull-left"></i>
  </button>
  
<% end %>
