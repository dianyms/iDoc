<%= form_for(glossary) do |f| %>
  <% if glossary.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(glossary.errors.count, "error") %> prohibited this glossary from being saved:</h2>

      <ul>
      <% glossary.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <label>Termo</label>
    <%= f.text_field :term, class:"form-control" %>
  </div>

  <div class="field">
    <label>Descrição</label>
    <%= f.text_area :description, class:"form-control" %>
  </div>

  <div class="form-group">
    <label>Sinônimo</label>
    <%= f.text_field :synonymous, class:"form-control" %>
  </div>

  <button type="submit" class="btn btn-success">Salvar<i class="fa fa-check pull-left"></i>
  </button>
  
<% end %>
