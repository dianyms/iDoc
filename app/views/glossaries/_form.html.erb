<%= form_for(glossary) do |f| %>
   <% if glossary.errors.any? %>
    <div class="alert alert-danger alert-dismissable">
      <button type="button" class="close" data-dismiss="alert" aria-hidden="true">×</button>
      <h2><%= pluralize(glossary.errors.count, "error") %> prohibited this project_user from being saved:</h2>

      <ul>
      <% glossary.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
  </div>
  <% end %>

  <div class="field">
    <label>Termo</label>
    <%= f.text_field :term, class:"form-control" %>
  </div>

  <div class="field">
    <label>Descrição</label>
    <%= f.text_area :description, class:"form-control" %>
  </div>

  <div class="form-group">
    <label>Sinônimo</label>
    <%= f.text_field :synonymous, class:"form-control" %>
  </div>

   <button type="submit" class="btn btn-primary">Salvar<i class="fa fa-check pull-left"></i>
  </button>
  
  <button class="btn btn-default" data-disable-with="Cancel" formnovalidate="" name="_continue" type="submit">
    <i class="fa fa-times"></i>
    Cancel
  </button>
  
<% end %>
